# Telegram Bot Configuration
# Get this from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Your Telegram user ID (admin)
# Send /start to @userinfobot to get your ID
TELEGRAM_ADMIN_ID=your_telegram_user_id

# ElevenLabs API Configuration
# Get from https://elevenlabs.io/app/settings
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Webhook Security
# Generate a long random path for webhook URL security
WEBHOOK_PATH=random_webhook_path_here_use_at_least_32_chars

# Generate a strong random secret for webhook validation
WEBHOOK_SECRET=long_random_secret_here_use_at_least_64_chars

# Admin endpoints security token
# Generate a strong random token (32+ characters) for /health and /metrics endpoints
ADMIN_HTTP_TOKEN=admin_token_for_health_metrics_endpoints_32plus_chars

# Environment (staging or prod)
ENV=staging

# Server port (defaults to 8080 if not specified)
# WARNING: Do not change PORT inside the container; Caddy expects 8080
PORT=8080

# Enable test acknowledgements (for testing only)
ENABLE_TEST_ACKS=false

# Security Notes:
# - Never commit real secrets to git
# - Use strong random values for WEBHOOK_PATH, WEBHOOK_SECRET, ADMIN_HTTP_TOKEN
# - Keep these values secure and rotate them periodically
# - WEBHOOK_PATH should be at least 32 characters
# - WEBHOOK_SECRET should be at least 64 characters
# - ADMIN_HTTP_TOKEN should be at least 32 characters
# - Internal port is fixed at 8080 for consistency with Caddy